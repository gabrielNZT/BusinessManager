import businessmanager.api.Product

model {
    Product product
}

json {
    key product.id
    name product.name
    code product.code
    price product.price
    unity product.unity
    stock product.stock
    minStock product.minStock
    isEnabled product.isEnabled
}